#ifndef PRESENT_BITS
#define PRESENT_BITS

#include <stdio.h>
#include <stdint.h>
#include <string.h>

void get_bits(void *number, uint8_t byte_count);
void get_hex_string(char *msg);

#endif // PRESENT_BITS
